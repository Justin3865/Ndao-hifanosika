import { clsx, type ClassValue } from "clsx"; import { twMerge } from "tailwind-merge"; /** * Combine les classes CSS et Tailwind. */ export function cn(...inputs: ClassValue[]): string { return twMerge(clsx(inputs)); } /** * Génère un identifiant unique. */ export function generateId(): string { return ( Math.random().toString(36).substring(2) + Date.now().toString(36) ); } /** * Tronque un texte à une longueur maximale. */ export function truncateText( text: string, maxLength: number = 50 ): string { if (text.length <= maxLength) { return text; } return text.substring(0, maxLength) + "..."; } /** * Capitalise un texte. */ export function capitalize(text: string): string { if (!text) { return ""; } return text.charAt(0).toUpperCase() + text.slice(1).toLowerCase(); } /** * Formate un nombre selon le format français. */ export function formatNumber( num: number, decimals: number = 0 ): string { return new Intl.NumberFormat("fr-FR", { minimumFractionDigits: decimals, maximumFractionDigits: decimals, }).format(num); } /** * Formate un pourcentage. */ export function formatPercentage( value: number, decimals: number = 0 ): string { return `${formatNumber(value, decimals)}%`; } /** * Calcule un pourcentage. */ export function calculatePercentage( value: number, total: number ): number { if (total === 0) { return 0; } return (value / total) * 100; } /** * Vérifie si une adresse email est valide. */ export function isValidEmail(email: string): boolean { const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/; return regex.test(email); } /** * Vérifie si un numéro de téléphone est valide. */ export function isValidPhone(phone: string): boolean { const regex = /^[+]?[0-9]{3}[-\s.]?[0-9]{3}[-\s.]?[0-9]{4,6}$/; return regex.test(phone); } /** * Vérifie si une URL est valide. */ export function isValidUrl(url: string): boolean { try { new URL(url); return true; } catch { return false; } } /** * Extrait le domaine d'une URL. */ export function getDomainFromUrl(url: string): string { try { const parsedUrl = new URL(url); return parsedUrl.hostname; } catch { return url; } } /** * Télécharge un fichier depuis un Blob. */ export function downloadFile( data: Blob, filename: string ): void { const url = URL.createObjectURL(data); const link = document.createElement("a"); link.href = url; link.download = filename; document.body.appendChild(link); link.click(); document.body.removeChild(link); URL.revokeObjectURL(url); } /** * Copie du texte dans le presse-papiers. */ export async function copyToClipboard( text: string ): Promise<boolean> { try { await navigator.clipboard.writeText(text); return true; } catch { return false; } } /** * Debounce une fonction. * * La fonction est exécutée uniquement après * que l'utilisateur a arrêté de l'appeler * pendant le délai spécifié. */ export function debounce< T extends (...args: any[]) => any >( fn: T, delay: number = 300 ): (...args: Parameters<T>) => void { let timeoutId: ReturnType<typeof setTimeout> | undefined; return (...args: Parameters<T>): void => { if (timeoutId !== undefined) { clearTimeout(timeoutId); } timeoutId = setTimeout(() => { fn(...args); }, delay); }; } /** * Throttle une fonction. * * La fonction ne peut être exécutée qu'une fois * pendant chaque période définie par "limit". */ export function throttle< T extends (...args: any[]) => any >( fn: T, limit: number = 300 ): (...args: Parameters<T>) => void { let inThrottle = false; return (...args: Parameters<T>): void => { if (inThrottle) { return; } fn(...args); inThrottle = true; setTimeout(() => { inThrottle = false; }, limit); }; } /** * Groupe un tableau selon une clé. */ export function groupBy< T extends Record<string, any> >( array: T[], key: keyof T ): Record<string, T[]> { return array.reduce( ( result: Record<string, T[]>, item: T ): Record<string, T[]> => { const groupKey = String(item[key]); if (!result[groupKey]) { result[groupKey] = []; } result[groupKey].push(item); return result; }, {} ); } /** * Trie un tableau selon une clé. */ export function sortBy< T extends Record<string, any> >( array: T[], key: keyof T, order: "asc" | "desc" = "asc" ): T[] { return [...array].sort( (a: T, b: T): number => { const aValue = a[key]; const bValue = b[key]; if (aValue < bValue) { return order === "asc" ? -1 : 1; } if (aValue > bValue) { return order === "asc" ? 1 : -1; } return 0; } ); } /** * Filtre un tableau selon une clé et une valeur. */ export function filterBy< T extends Record<string, any> >( array: T[], key: keyof T, value: any ): T[] { return array.filter( (item: T): boolean => item[key] === value ); } /** * Recherche dans un tableau. * * Recherche le terme dans les propriétés indiquées. */ export function searchInArray< T extends Record<string, any> >( array: T[], searchTerm: string, keys: Array<keyof T> ): T[] { const term = searchTerm.trim().toLowerCase(); if (!term) { return array; } return array.filter( (item: T): boolean => { return keys.some( (key: keyof T): boolean => { const value = item[key]; if (value === null || value === undefined) { return false; } return String(value) .toLowerCase() .includes(term); } ); } ); }